# FlashFusion SoT Monorepo - Root Environment Variables
# Copy to .env and customize for your local development environment

# =============================================================================
# Node Environment
# =============================================================================
NODE_ENV=development

# =============================================================================
# GitHub Configuration (for import scripts and CI/CD)
# =============================================================================
# GitHub Personal Access Token (for importing repositories)
# Scopes needed: repo, read:org
# Generate at: https://github.com/settings/tokens/new
GITHUB_TOKEN=your_github_personal_access_token

# Repository for deploy keys configuration
DEPLOY_KEYS_REPO=Krosebrook/source-of-truth-monorepo

# =============================================================================
# Turborepo Remote Caching (Optional - for team caching)
# =============================================================================
# Get these from: https://vercel.com/account/tokens
# TURBO_TOKEN=your_vercel_token
# TURBO_TEAM=your_team_slug

# Remote cache configuration
# TURBO_REMOTE_CACHE_ENABLED=true

# =============================================================================
# Development Services (Local Docker/Database)
# =============================================================================
# PostgreSQL (used by docker-compose.yml)
POSTGRES_DB=flashfusion_dev
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev_password_change_me
POSTGRES_PORT=5432

# Redis (caching)
REDIS_PORT=6379
REDIS_PASSWORD=

# =============================================================================
# Project-Specific Environment Variables
# =============================================================================
# Each project may have its own .env.example file in its directory.
# See:
#   - projects/local/flashfusion-consolidated/packages/ai-core/.env.example
#   - projects/local/flashfusion-consolidated/packages/shared/.env.example
#   - projects/local/int-smart-triage-ai-2.0/.env.example

# =============================================================================
# Monitoring & Observability (Optional)
# =============================================================================
# Sentry (error tracking)
# SENTRY_DSN=your_sentry_dsn
# SENTRY_ORG=your_sentry_org
# SENTRY_PROJECT=your_sentry_project

# OpenTelemetry (distributed tracing)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SERVICE_NAME=flashfusion-sot

# =============================================================================
# CI/CD Secrets (GitHub Actions Only - DO NOT set locally)
# =============================================================================
# These are managed via GitHub Secrets and should NOT be in your local .env:
#   - MIRROR_SSH_KEY_* (50 deploy keys for mirror repositories)
#   - DEPLOY_KEY_PASSPHRASE (if keys are encrypted)

# =============================================================================
# Security Notes
# =============================================================================
# 1. NEVER commit the actual .env file to version control (.gitignore excludes it)
# 2. Rotate tokens regularly and after any suspected compromise
# 3. Use different credentials for development, staging, and production
# 4. Store production secrets in secure secret management (GitHub Secrets, Vercel, etc.)
# 5. Review the SECURITY.md file for complete security guidelines

# =============================================================================
# Getting Started
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values (especially GITHUB_TOKEN if importing repos)
# 3. Keep your .env file private and secure
# 4. See GETTING_STARTED.md for complete setup instructions
