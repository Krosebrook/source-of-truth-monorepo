═══════════════════════════════════════════════════════
  MCP SERVERS - QUICK START GUIDE
═══════════════════════════════════════════════════════

✅ INSTALLED (14 MCP Servers):
  1. filesystem          - File operations
  2. memory              - Context persistence  
  3. sequential-thinking - Enhanced reasoning
  4. everything          - Comprehensive toolkit
  5. postgres            - PostgreSQL database
  6. mongodb             - MongoDB database
  7. notion              - Notion workspace
  8. puppeteer           - Browser automation
  9. chrome-devtools     - Chrome debugging
  10. context7           - Doc search
  11. apify              - Web scraping
  12. heroku             - Platform deployment
  13. sentry             - Error tracking (SSE)
  14. cloud-k8s          - Kubernetes (local)

📄 CONFIGURATION FILES:
  ~/.mcp.json              - Main config (CREATED ✓)
  ~/.mcp.env.example       - Credentials template (CREATED ✓)
  ~/MCP_SERVERS_README.md  - Full documentation (CREATED ✓)
  ~/setup-mcp.sh           - Setup utility (CREATED ✓)
  ~/source-of-truth-monorepo/.mcp.json - Project config (CREATED ✓)

🚀 QUICK START:
  1. Setup credentials:
     cp ~/.mcp.env.example ~/.mcp.env
     nano ~/.mcp.env  # Add your API keys
     
  2. Load environment:
     source ~/.mcp.env
     
  3. Test setup:
     ./setup-mcp.sh
     
  4. Use with Claude:
     claude  # Auto-loads ~/.mcp.json
     
  5. Project-specific:
     cd ~/source-of-truth-monorepo
     claude  # Loads project .mcp.json

🔑 CREDENTIALS NEEDED (optional):
  □ GITHUB_TOKEN             - GitHub API access
  □ POSTGRES_CONNECTION_STRING - Database URL
  □ MONGODB_CONNECTION_STRING  - MongoDB URL
  □ NOTION_API_KEY           - Notion integration
  □ APIFY_API_TOKEN          - Apify platform
  □ HEROKU_API_KEY           - Heroku CLI

📖 DOCUMENTATION:
  Full guide: cat ~/MCP_SERVERS_README.md
  Run setup:  ./setup-mcp.sh
  Test MCP:   npx @modelcontextprotocol/inspector

═══════════════════════════════════════════════════════
