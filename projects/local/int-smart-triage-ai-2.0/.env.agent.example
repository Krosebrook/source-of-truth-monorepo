# Agent runtime integrations
AGENT_DASH_WEBHOOK=https://dashboard.example.com/hooks/agent-status
AGENT_DASH_TOKEN=replace-with-dashboard-token
AUTOMATION_API_URL=https://automation.example.com/api/agent-events
AUTOMATION_API_TOKEN=replace-with-automation-token

# Automation command scheduling
# Command the automation agent should execute when active
AUTOMATION_VALIDATE_COMMAND=npm run validate
# Interval in milliseconds between automation command executions (e.g., 86400000 for 24h)
AUTOMATION_VALIDATE_INTERVAL_MS=86400000
